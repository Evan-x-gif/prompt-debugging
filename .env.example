# API 配置示例
# 复制此文件为 .env 并填入你的实际配置

# API 基础 URL（可选，应用内可配置）
VITE_API_BASE_URL=https://api.openai.com

# API Key（可选，应用内可配置）
VITE_API_KEY=your-api-key-here

# 默认模型 ID（可选，应用内可配置）
VITE_MODEL_ID=gpt-4

# 注意：
# 1. 这些环境变量是可选的，你也可以直接在应用的设置面板中配置
# 2. 应用会将配置保存在浏览器的 localStorage 中
# 3. 请勿将包含真实 API Key 的 .env 文件提交到 Git
